# Скопируй в .env и заполни. Файл .env не коммитится в git.
# Токен: https://vercel.com/account/tokens
VERCEL_TOKEN=
MINIMAX_API_KEY=
# Имя проекта на Vercel (как в URL), например myfirstrepo
VERCEL_PROJECT_NAME=myfirstrepo
